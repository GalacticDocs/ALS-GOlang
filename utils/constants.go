package als_utils

const (
	/* Base URL for the ALS (ApexLegendsStatus) API. */
	BASE = "https://api.mozambiquehe.re/bridge?version=5"
)
const (
	/* Identifier for the PC platform. */
	Origin = "PC"
	/* Identifier for the XBox platform. */
	XBox = "X1"
	/* Identifier for the playstation platform. */
	Playstation = "PS4"
)

const (
	/* Identifier for the general statistics. */
	GeneralStats = "general"
	/* Identifier for the advanced statistics. */
	AdvancedStats = "advanced"
	/* Identifier for both/all statistics. */
	BothStats = "both"
)
